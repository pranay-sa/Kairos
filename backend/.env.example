# Groq (chat completions)
GROQ_API_KEY=gsk_...
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_CHAT_MODEL=llama-3.1-8b-instant

# Local embeddings (no key required)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Qdrant (docker-compose defaults)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=kairos_documents

# Retrieval / grounding
CONFIDENCE_THRESHOLD=0.45
VECTOR_TOP_K=8

# GitHub PR automation (optional)
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
GITHUB_DEFAULT_BRANCH=main

# Webhooks (optional verification)
SLACK_SIGNING_SECRET=
JIRA_WEBHOOK_SECRET=

# Jira (optional) - used for backfill + proper browse links
JIRA_BASE_URL=https://example.atlassian.net
JIRA_EMAIL=
JIRA_API_TOKEN=

# Jira backfill on startup (optional)
JIRA_BACKFILL_ON_STARTUP=false
JIRA_BACKFILL_JQL=project=SCRUM order by updated desc
JIRA_BACKFILL_MAX_ISSUES=200

# Azure Monitor / Log Analytics -> Qdrant (optional)
# Create an App Registration + Client Secret; grant it Log Analytics Reader on the workspace.
AZURE_MONITOR_ENABLED=false
AZURE_POLL_INTERVAL_MINUTES=20
AZURE_STATE_PATH=data/azure_monitor_state.json

AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

AZURE_LOG_ANALYTICS_WORKSPACE_ID=
AZURE_LOG_ANALYTICS_ENDPOINT=https://api.loganalytics.io
# Your KQL. Keep it time-agnostic; the poller passes a timespan window.
AZURE_LOG_ANALYTICS_QUERY=AppTraces | project TimeGenerated, SeverityLevel, Message

# Microsoft Teams chats -> Qdrant (optional)
# Uses Microsoft Graph API with client credentials.
TEAMS_SYNC_ENABLED=false
TEAMS_POLL_INTERVAL_MINUTES=10
TEAMS_STATE_PATH=data/teams_graph_state.json

MS_TENANT_ID=
MS_CLIENT_ID=
MS_CLIENT_SECRET=

# Comma-separated user IDs or UPNs to sync chats for
TEAMS_USER_IDS=user1@yourtenant.com,user2@yourtenant.com
